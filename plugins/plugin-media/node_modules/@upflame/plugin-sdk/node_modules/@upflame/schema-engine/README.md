# @upflame/schema-engine

Shared schema contracts and builders for the Upflame CMS ecosystem.

This package is the first extraction step toward a standalone content model layer. It contains the composition DSL plus CMS document schemas that were previously only available through the root runtime package.
