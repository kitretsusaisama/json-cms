# @upflame/cli-utils

Shared CLI helpers for scaffolding and install orchestration.

## Scope

- package manager detection and install command rendering
- project manifest generation
- CMS config rendering helpers
- token replacement and template copy helpers
- preset metadata helpers used by generators
